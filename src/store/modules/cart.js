let state = {
  selectedGoods: []
}
const mutations = {
  selectedGoods: state => state.selectedGoods
}
const actions = {}
const getters = {
  selectedGoods: state => state.selectedGoods
}
export default {
  state,
  getters,
  actions,
  mutations
}
